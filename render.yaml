services:
  - type: web
    name: shortsmaker
    runtime: node
    plan: starter
    buildCommand: apt-get update && apt-get install -y ffmpeg && npm install
    startCommand: npm start
    autoDeploy: true
